# Quench Config

Forge's small shared configuration-loading helper: reading JSON/YAML/TOML files and typed environment variables through one API instead of every service hand-rolling `std::env::var` + `.parse()`.

See [docs/libs/quench-config.md](../../docs/libs/quench-config.md) for full documentation.
