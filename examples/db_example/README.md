# DB Example

Runnable reference for `quench-db`'s migration runner and CRUD repository (package `quench-db-example`) against a real Postgres database. Loads Liquibase-style migrations from `migrations/` and exercises create/list/update/delete through `PostgresRepository`.

See [docs/examples/db_example.md](../../docs/examples/db_example.md) for full documentation.
