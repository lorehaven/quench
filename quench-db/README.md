# Quench DB

Small ORM/database abstraction used across Forge's Actix services — a generic `Database` trait, `Model`/`Crud` for Postgres or in-memory CRUD, and the versioned migration catalog Foundry drives.

See [docs/quench-db.md](../docs/quench-db.md) for full documentation.
