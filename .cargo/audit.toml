[advisories]
ignore = [
    # h2 unbounded empty DATA frames (low severity DoS), fixed only in h2 >=0.4.16.
    # Pulled in transitively via actix-http 3.13.3 -> actix-web 4.14.1, which is
    # the latest actix-web release and still pins h2 to the 0.3 line, via
    # quench-auth/quench-starter. No action possible until actix-web ships a
    # release on h2 0.4+/1.x. Same finding as forge's own .cargo/audit.toml -
    # this is the same actix-web pin, reached from this workspace's own crates.
    # Re-check: https://rustsec.org/advisories/RUSTSEC-2026-0258
    "RUSTSEC-2026-0258",
]
