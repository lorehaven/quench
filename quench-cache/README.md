# Quench Cache

Shared caching layer for Forge services — in-process by default, Redis (standalone or cluster) behind the `redis` feature, one `CacheStore` API either way.

See [docs/quench-cache.md](../docs/quench-cache.md) for full documentation.
