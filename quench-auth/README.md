# Quench Auth

The relying-party half of Forge authentication: everything a service needs to *verify* a realm session and hand a browser to gatehouse for login. It deliberately does not contain the auth API, login form, or user administration — those live in gatehouse.

See [docs/libs/quench-auth.md](../../docs/libs/quench-auth.md) for full documentation.
