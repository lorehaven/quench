# Quench Log

One `tracing` subscriber for every service and CLI in the estate — plain-text to the console, rolling JSON (by default) to disk, both at once, configured from environment variables.

See [docs/quench-log.md](../docs/quench-log.md) for full documentation.
